version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - TZ=Asia/Kuala_Lumpur
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
